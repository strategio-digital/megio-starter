{
    "name": "strategio/megio-starter",
    "description": "Boilerplate for Megio apps & webs",
    "type": "project",
    "license": "MIT",
    "autoload": {
        "psr-4": {
            "App\\": "app/",
            "App\\Migrations\\": "migrations/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "authors": [
        {
            "name": "Jiří Zapletal",
            "email": "jz@strategio.dev"
        }
    ],
    "minimum-stability": "stable",
    "require": {
        "php": "^8.4",
        "strategio/megio-core": "dev-master",
        "symfony/serializer": "^7.3",
        "symfony/validator": "^7.3",
        "symfony/property-access": "^7.3"
    },
    "require-dev": {
        "phpunit/phpunit": "^12.0",
        "friendsofphp/php-cs-fixer": "^v3.87.2",
        "phpstan/phpstan": "^2.1"
    },
    "scripts": {
        "phpstan:baseline": "php -d memory_limit=-1 vendor/bin/phpstan analyse -c phpstan.neon -vv -b phpstan-baseline.neon --allow-empty-baseline",
        "phpstan": "php -d memory_limit=-1 vendor/bin/phpstan analyse -c phpstan.neon",
        "neon": "vendor/bin/neon-lint config/",
        "latte": "vendor/bin/latte-lint view/",
        "schema": "bin/console orm:validate-schema -v",
        "test": "vendor/bin/phpunit",
        "test:unit": "vendor/bin/phpunit tests/Unit",
        "test:feature": "vendor/bin/phpunit tests/Feature",
        "test:coverage": "vendor/bin/phpunit --coverage-html coverage",
        "format": "vendor/bin/php-cs-fixer fix --config=.php-cs-fixer.php",
        "format:check": "vendor/bin/php-cs-fixer fix --config=.php-cs-fixer.php --dry-run --diff",
        "analyse": [
            "@phpstan",
            "@format:check",
            "@neon",
            "@latte",
            "@schema",
            "@test"
        ]
    }
}
